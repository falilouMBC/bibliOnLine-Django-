# django_Project
    #initialize project
        #venv
        py -m virtualenv env
        #django_Project requirement 
        pip install -r path/to/requirement.text
        #import database
        py manage.py migrate
        #
    #test

